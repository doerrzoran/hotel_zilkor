<form method="post" action="{{ path('app_hostel_crud_delete', {'id': hostel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hostel.id) }}">
    <button class="btn">Delete</button>
</form>
